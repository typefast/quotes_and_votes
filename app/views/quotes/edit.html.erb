<%= form_for @quote, url: {action: "update"} do |f| %>
  
  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.label :author %>
  <%= f.text_field :author %>
  
  <%= f.submit "Submit" %>

<% end %>